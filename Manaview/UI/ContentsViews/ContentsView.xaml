<UserControl x:Class="Manaview.UI.ContentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Manaview.UI"
             mc:Ignorable="d" 
             
             x:Name="ui_ContentsView"
             d:DesignHeight="800" d:DesignWidth="1000">

    <!--<DataTemplate DataType="{x:Type local:ImageItemVM}">
        <local:ImageItemView/>
    </DataTemplate>-->

    <Grid Background="{StaticResource mainContentsBG}">
        <ContentControl Content="{Binding ImageViewer}"></ContentControl>
    </Grid>

</UserControl>
